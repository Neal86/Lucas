async function render(){const s=await chrome.storage.local.get(["paired","pairingCode","profileName","profileId","alias","browserType"]);const st=document.getElementById("status");st.textContent=s.paired?"Connected to Lucas":"Waiting for Lucas pairing";st.className="row "+(s.paired?"ok":"warn");document.getElementById("profile").textContent=[s.browserType||"ixbrowser",s.profileName||s.alias||"",s.profileId?"#"+s.profileId:""].filter(Boolean).join(" · ");document.getElementById("code").textContent=!s.paired&&s.pairingCode?"Pairing code: "+s.pairingCode:"";}document.getElementById("options").onclick=()=>chrome.runtime.openOptionsPage();render();