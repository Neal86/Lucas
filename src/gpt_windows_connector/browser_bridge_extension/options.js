const ids=["browserType","profileName","profileId","alias","tags","groups"];async function load(){const s=await chrome.storage.local.get(ids);for(const id of ids){const el=document.getElementById(id);const v=s[id];el.value=Array.isArray(v)?v.join(", "):(v||"");}}document.getElementById("save").onclick=async()=>{const data={};for(const id of ids){const v=document.getElementById(id).value.trim();data[id]=(id==="tags"||id==="groups")?v.split(",").map(x=>x.trim()).filter(Boolean):v;}await chrome.storage.local.set(data);document.getElementById("result").textContent="Saved. Reconnecting to Lucas…";};load();