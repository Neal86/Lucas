name: Integrate referral dashboard
on:
  push:
    branches: [main]
    paths:
      - 'scripts/integrate_referral_dashboard.py'
      - '.github/workflows/integrate-referral-dashboard.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply referral dashboard changes
        run: python scripts/integrate_referral_dashboard.py
      - name: Fix generated referral compatibility
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('src/gpt_windows_connector/web_dashboard_markup.py')
          s=p.read_text()
          bad="onclick=\"view('referral',this)\""
          good="onclick=\"view(\\'referral\\',this)\""
          if bad not in s:
              raise SystemExit('generated referral nav marker not found')
          p.write_text(s.replace(bad,good,1))

          p=Path('src/gpt_windows_connector/referrals.py')
          s=p.read_text()
          old="INSERT OR IGNORE INTO subscriptions(user_id,plan,status,updated_at) VALUES(?, 'free','inactive',?)"
          new="INSERT OR IGNORE INTO subscriptions(user_id,updated_at) VALUES(?,?)"
          if s.count(old) != 2:
              raise SystemExit('generated subscription compatibility markers not found')
          p.write_text(s.replace(old,new))
          PY
      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e '.[dev]'
      - name: Compile
        run: python -m compileall -q src
      - name: Test
        run: pytest -q
      - name: Cleanup one-shot files
        run: |
          rm scripts/integrate_referral_dashboard.py
          rm .github/workflows/integrate-referral-dashboard.yml
      - name: Commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m 'Integrate Refer and Earn into dashboard and update rewards'
          git push origin HEAD:main
